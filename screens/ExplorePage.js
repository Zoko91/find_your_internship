import React, { useState, useEffect } from "react";
import {
  Text,
  View,
  Image,
  TextInput,
  FlatList,
  TouchableOpacity,
} from "react-native";

const ExplorePage = ({ navigation }) => {

  return (
    <View>
        <Text>Hello world ! This is the explore Page</Text>
    </View>
  );
};

export default ExplorePage;